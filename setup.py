# !/usr/bin/env python
import setuptools


if __name__ == "__main__":
    # setuptools.setup(use_scm_version=False, setup_requires=["setuptools_scm"])
    # setuptools.setup(version="0.0.2")
    setuptools.setup()